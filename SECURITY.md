# 安全说明

## 报告安全问题

请不要在公开 Issue 中提交密码、会话令牌、数据库、服务器地址或可复现的利用细节。请通过 GitHub Security Advisory 的私密报告功能联系仓库维护者。

## 部署要求

- 不要提交 `.env`、数据库目录、行情快照、日志、部署归档或本机凭据。
- 生产环境应只让 Node 服务监听回环地址，并通过 Nginx 或同类反向代理提供 HTTPS。
- 自签名证书仅适合测试；公开服务应使用受信任证书。
- 注册、登录和交易接口应配置限流，并定期清理过期会话。
- 真实行情接口只用于模拟交易；本项目不会连接真实券商。

## 历史密钥

仓库早期版本曾包含示例 API Key。它们不属于当前架构，也不得在任何环境继续使用；从旧版本部署升级时必须重新生成所有密钥和会话。
